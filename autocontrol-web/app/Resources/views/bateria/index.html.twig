{% extends 'base.html.twig' %}

{% block body %}
    <legend>Listado de Baterias</legend>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Energia</th>
                <th>Temperatura</th>
                <th>Coordenada</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for baterium in baterias %}
            <tr>
                <td><a href="{{ path('bateria_show', { 'id': baterium.id }) }}">{{ baterium.id }}</a></td>
                <td>{{ baterium.numero }}</td>
                <td>{{ baterium.energia }}</td>
                <td>{{ baterium.temperatura }}</td>
                <td>{{ baterium.coordenada }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('bateria_show', { 'id': baterium.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a href="{{ path('bateria_edit', { 'id': baterium.id }) }}">Editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-primary" href="{{ path('bateria_new') }}">Crear nueva Batería</a>
        </li>
    </ul>
{% endblock %}
